cmake_minimum_required(VERSION 3.20)
project(MalGuardSandboxContainmentProbe LANGUAGES CXX)
add_executable(MalGuardSandboxContainmentProbe MalGuardSandboxContainmentProbe.cpp)
target_compile_features(MalGuardSandboxContainmentProbe PRIVATE cxx_std_17)
target_compile_definitions(MalGuardSandboxContainmentProbe PRIVATE UNICODE _UNICODE WIN32_LEAN_AND_MEAN NOMINMAX)
if(MSVC)
  target_compile_options(MalGuardSandboxContainmentProbe PRIVATE /W4 /WX /guard:cf /sdl)
  target_link_options(MalGuardSandboxContainmentProbe PRIVATE /DYNAMICBASE /NXCOMPAT /HIGHENTROPYVA /guard:cf)
endif()
